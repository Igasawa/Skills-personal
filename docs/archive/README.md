# Archive Directory

このディレクトリは、**現行運用では参照しない過去ドキュメント**を保管するための場所です。

## 運用ルール

- 現行仕様・現行手順として参照する文書は `docs/` 直下に置く。
- 一度きりの計画書・MVPタスク分解・作業引き継ぎメモなど、更新停止した文書は `docs/archive/legacy/` に移動する。
- アーカイブ時は「なぜ現行でないか」をコミットメッセージまたはPR本文に残す。

## 今回アーカイブした文書（2026-02-21）

- `automation_jobs_handover_2026-02-18.md`（単発の引き継ぎメモ、文字化け混入あり）
- `shared-config-plan.md`（移行計画フェーズ文書。現行は `shared-config-resolution.md` / `shared-config-runbook.md` を参照）
- `workflow_template_mvp_spec.md` / `workflow_template_mvp_implementation_tasks.md`（MVP実装時の一時仕様）
- `workflow_step_trigger_execution_mvp_spec.md` / `workflow_step_trigger_execution_mvp_tasks.md`（MVP実装時の一時仕様）
- `structure-refactor-roadmap.md`（実行フェーズ終了後のロードマップ）
